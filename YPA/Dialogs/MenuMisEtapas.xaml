<?xml version="1.0" encoding="utf-8" ?>

<Frame xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:prism="http://prismlibrary.com"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:local="clr-namespace:YPA.Views.DataTemplates"
             x:Class="YPA.Dialogs.MenuMisEtapas">

    <StackLayout Orientation="Vertical"
                 BackgroundColor="#F8EDED" HeightRequest="300" WidthRequest="250"
                 VerticalOptions="Center" Padding="4,0" Spacing="0">

        <Button Text="Insertar día al inicio" HorizontalOptions="Center"  Command="{Binding InsertarDiaAlInicio}" />
        <Button Text="Añadir día al final" HorizontalOptions="Center"  Command="{Binding CloseCommand}" />
        <Button Text="Dividir esta etapa en varias" HorizontalOptions="Center"  Command="{Binding CloseCommand}" />
        <Button Text="Unir esta etapa y la siguiente" HorizontalOptions="Center"  Command="{Binding CloseCommand}" />
        <Button Text="CLOSE" HorizontalOptions="Center"  Command="{Binding CloseCommand}" />

    </StackLayout>
    
</Frame>    