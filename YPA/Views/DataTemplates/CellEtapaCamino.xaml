<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converter="clr-namespace:YPA.ViewModels"
             mc:Ignorable="d"
             x:Class="YPA.Views.DataTemplates.CellEtapaCamino">


    <ViewCell.View>
        <StackLayout Orientation="Horizontal" Padding="0" Margin="10,0" Spacing="15"
                     IsEnabled="{Binding esVisible}" >
                     <!-- IsEnabled="{Binding nombrePoblacion, Converter={StaticResource poblacionVisible}}"> -->
            <StackLayout.Resources>
                <ResourceDictionary>
                    <converter:IntToColorStringConverter x:Key="intToColorString" />
                    <converter:Acumulado x:Key="acumulado" />
                    <converter:PoblacionVisible x:Key="poblacionVisible" />
                </ResourceDictionary>
            </StackLayout.Resources>
            <Label Text="{Binding id}" FontAttributes="Bold" 
                   TextColor="Red" 
                   Margin="0" Padding="0" HorizontalOptions="Center" AnchorX="10" 
                   BackgroundColor="{Binding IniBifurcacion,  Converter={StaticResource intToColorString}}" >

                <!-- <Label.Triggers>
                    <DataTrigger TargetType="Label" Binding="{Binding IniBifurcacion}" Value=1>
                        <Setter Property="BackgroundColor" Value="Yellow" />
                    </DataTrigger>
                </Label.Triggers> -->
            </Label>
            <Label Text="{Binding nombrePoblacion}" HorizontalOptions="Start" FontAttributes="Bold" MinimumWidthRequest="200"/>
            <Label Text="{Binding nodosSiguientes} " HorizontalOptions="Start" FontSize="12" />
            <Label Text="{Binding distanciaNodosSiguientes} " AnchorX="30" HorizontalOptions="End" />
            <Label Text="{Binding acumulado, StringFormat='{0}km'}" AnchorX="30" HorizontalOptions="EndAndExpand" />
            <!-- <Label Text="{Binding distanciaNodosSiguientes, StringFormat='{0}km', Converter={StaticResource acumulado}}" AnchorX="30" HorizontalOptions="EndAndExpand" /> -->
        </StackLayout>
    </ViewCell.View>
</ViewCell>